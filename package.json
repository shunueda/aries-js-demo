{
  "name": "root",
  "private": true,
  "workspaces": [
    "demo",
    "server"
  ],
  "scripts": {
    "format": "prettier --ignore-path .gitignore \"**/*.+(ts|js|tsx)\" --write"
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  }
}
